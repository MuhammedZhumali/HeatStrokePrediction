spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/heatstr
    username: heatstr
    password: heatstr
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: false
    change-log: classpath:db/db.changelog-master.xml
    default-schema: heatstr
  jpa:
      properties:
        hibernate.default_schema: heatstr
  security:
    user:
      name: admin
      password: admin
      roles: ADMIN